📯 Commitizen adapter by Blue Tomato.

# Installation
Type `npm install --save-dev commitizen`

then type `npx commitizen init @blue-tomato/cz-jira-conventional-changelog --save-dev` into your console
